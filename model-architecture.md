# सामान्य प्रयोजन न्यूरल नेटवर्क मॉडल आर्किटेक्चर डिज़ाइन

**संस्करण:** 1.0  
**प्रकार:** मॉड्यूलर Encoder–Backbone–Head आर्किटेक्चर  
**उपयोग:** वर्गीकरण (Classification) और अनुक्रम कार्य (Sequence Tasks) के लिए शुरुआती ब्लूप्रिंट

---

## 1. उद्देश्य / Overview

यह दस्तावेज़ एक **सामान्य-प्रयोजन (general-purpose)** आधुनिक डीप लर्निंग मॉडल आर्किटेक्चर प्रस्तुत करता है। कोई विशिष्ट डोमेन (Vision / NLP) तय नहीं होने पर भी यह डिज़ाइन एक मज़बूत शुरुआती बिंदु देता है।

### मुख्य लक्ष्य
- **मॉड्यूलर संरचना:** इनपुट, एम्बेडिंग, बैकबोन, अटेंशन/पूलिंग और टास्क हेड अलग-अलग बदले जा सकें
- **लचीलापन:** एक ही कंकाल से छवि वर्गीकरण, टेक्स्ट क्लासिफिकेशन, या अनुक्रम लेबलिंग तक विस्तार
- **स्पष्ट इंटरफ़ेस:** हर ब्लॉक के इनपुट/आउटपुट टेंसर आकार उच्च स्तर पर परिभाषित
- **ट्रेनिंग व इन्फरेंस दोनों** के लिए व्यावहारिक नोट्स

### आर्किटेक्चर नाम
**Modular Encoder–Backbone–Head (MEBH) Network**

सरलीकृत प्रवाह:

```
इनपुट → प्रीप्रोसेसिंग / एम्बेडिंग → बैकबोन (Encoder) → अटेंशन / पूलिंग → टास्क हेड → आउटपुट (भविष्यवाणी)
```

---

## 2. लेयर्स और कंपोनेंट्स

नीचे प्रत्येक प्रमुख ब्लॉक का विवरण है।

### 2.1 इनपुट लेयर (Input)

| गुण | विवरण |
|-----|--------|
| भूमिका | कच्चा डेटा मॉडल में लाना |
| उदाहरण आकार | बैच आकार `B` के साथ: छवि `(B, C, H, W)` या अनुक्रम `(B, T)` या `(B, T, F)` |
| नोट | पैडिंग, मास्किंग, और डेटाटाइप यहाँ तय होते हैं |

**इनपुट प्रकार (विशेषज्ञता से पहले):**
- **टेबुलर / फीचर वेक्टर:** `(B, F)`
- **अनुक्रम (टोकन आईडी):** `(B, T)`
- **छवि:** `(B, C, H, W)`
- **मल्टीमॉडल:** उपरोक्त का संयोजन (अलग एन्कोडर पथ)

### 2.2 एम्बेडिंग / प्रीप्रोसेसिंग (Embedding & Preprocessing)

| गुण | विवरण |
|-----|--------|
| भूमिका | कच्चे इनपुट को निरंतर वेक्टर स्थान (dense representation) में बदलना |
| आउटपुट आकार | सामान्यतः `(B, T, D)` या `(B, N, D)` जहाँ `D` = मॉडल आयाम (जैसे 256 / 512 / 768) |

**उप-कंपोनेंट्स:**
1. **Token / Patch Embedding** — टेक्स्ट टोकन या इमेज पैच को `D`-आयामी वेक्टर में मैप
2. **Positional Encoding** — क्रम / स्थान की जानकारी जोड़ना (सिनसॉइडल या सीखने योग्य)
3. **Normalization / Dropout** — स्थिर ट्रेनिंग के लिए (LayerNorm + Dropout)
4. **Optional Projection** — इनपुट फीचर आयाम को `D` पर लाना (`Linear` / `Conv1×1`)

### 2.3 बैकबोन / एनकोडर (Backbone Encoder)

| गुण | विवरण |
|-----|--------|
| भूमिका | संदर्भ-संपन्न विशेषताएँ (contextual features) निकालना |
| संरचना | `L` स्टैक्ड ब्लॉक (Transformers ब्लॉक या Residual CNN ब्लॉक) |
| आउटपुट आकार | `(B, T, D)` — अनुक्रम संरक्षित; या स्टेज-वाइज़ डाउनसैंपल के साथ `(B, D', H', W')` |

**अनुशंसित सामान्य ब्लॉक — Transformer Encoder Block:**
1. Multi-Head Self-Attention (MHSA)
2. Add & LayerNorm (Pre-Norm अनुशंसित)
3. Feed-Forward Network (FFN): `Linear → GELU → Dropout → Linear`
4. Add & LayerNorm

**वैकल्पिक बैकबोन विकल्प:**
- **CNN Residual Stack** — Vision के लिए (ResNet-शैली)
- **Hybrid** — CNN stem + Transformer encoder
- **Lightweight MLP / ConvNeXt** — कम संसाधन वाले परिदृश्य

**सुझाए गए हाइपरपैरामीटर (शुरुआत):**

| पैरामीटर | छोटा मॉडल | मध्यम मॉडल |
|----------|-----------|-------------|
| `D` (मॉडल आयाम) | 256 | 512 |
| Heads (`H`) | 4 | 8 |
| Layers (`L`) | 4–6 | 8–12 |
| FFN hidden | `4 × D` | `4 × D` |
| Dropout | 0.1 | 0.1 |

### 2.4 अटेंशन / पूलिंग (Attention & Pooling)

| गुण | विवरण |
|-----|--------|
| भूमिका | चर-लंबाई अनुक्रम / फीचर मैप को निश्चित आकार के वेक्टर में समेटना |
| आउटपुट आकार | `(B, D)` वैश्विक प्रतिनिधित्व के लिए |

**विकल्प:**
1. **CLS Token** — पहले टोकन को सारांश के रूप में उपयोग
2. **Mean / Max Pooling** — समय / स्थान आयाम पर औसत या अधिकतम
3. **Attention Pooling** — सीखने योग्य क्वेरी से weighted sum
4. **Global Average Pooling (GAP)** — Vision फीचर मैप के लिए

टास्क के अनुसार यह ब्लॉक वैकल्पिक हो सकता है (उदा. टोकन-स्तरीय लेबलिंग में पूलिंग नहीं)।

### 2.5 टास्क हेड्स (Task Heads)

| गुण | विवरण |
|-----|--------|
| भूमिका | बैकबोन प्रतिनिधित्व को कार्य-विशिष्ट आउटपुट में बदलना |
| इनपुट | `(B, D)` या `(B, T, D)` |

**सामान्य हेड प्रकार:**

| हेड | इनपुट | आउटपुट | उपयोग |
|-----|--------|---------|--------|
| Classification Head | `(B, D)` | `(B, K)` logits | K-क्लास वर्गीकरण |
| Sequence Labeling Head | `(B, T, D)` | `(B, T, K)` | NER, POS टैगिंग |
| Regression Head | `(B, D)` | `(B, R)` | संख्यात्मक भविष्यवाणी |
| Multi-label Head | `(B, D)` | `(B, K)` + Sigmoid | कई लेबल एक साथ |

**Classification Head का सरल रूप:**
```
LayerNorm → Dropout → Linear(D → K)
```

### 2.6 आउटपुट लेयर (Output)

| गुण | विवरण |
|-----|--------|
| भूमिका | लॉगिट्स से भविष्यवाणी / संभावना |
| ट्रेनिंग | अक्सर Softmax / Sigmoid लॉस के अंदर ही लगता है (numerically stable) |
| इन्फरेंस | Softmax / Argmax / Threshold लागू करके अंतिम लेबल |

**आउटपुट उदाहरण:**
- वर्ग सूचकांक: `argmax(logits)` → आकार `(B,)`
- संभावनाएँ: `softmax(logits)` → आकार `(B, K)`
- अनुक्रम लेबल: प्रति टाइमस्टेप `argmax` → `(B, T)`

---

## 3. डेटा फ्लो (Input → Prediction)

```
[1] कच्चा इनपुट
        ↓
[2] प्रीप्रोसेसिंग / एम्बेडिंग  →  X ∈ ℝ^{B×T×D}
        ↓
[3] बैकबोन (L × Encoder Block)  →  H ∈ ℝ^{B×T×D}
        ↓
[4] अटेंशन / पूलिंग (वैकल्पिक)  →  z ∈ ℝ^{B×D}
        ↓
[5] टास्क हेड  →  logits ∈ ℝ^{B×K}   (या B×T×K)
        ↓
[6] आउटपुट सक्रियण / डिकोडिंग  →  पूर्वानुमान ŷ
```

### चरण-दर-चरण संक्षेप
1. बैच डेटा लोड → टेंसर बनाना  
2. एम्बेड + पोज़िशन → घनत्वपूर्ण अनुक्रम  
3. सेल्फ-अटेंशन / residual ब्लॉकों से संदर्भ सीखना  
4. वैश्विक वेक्टर बनाना (अगर कार्य वैश्विक है)  
5. रैखिक प्रक्षेपण से क्लास लॉगिट्स  
6. लॉस गणना (ट्रेनिंग) या लेबल चयन (इन्फरेंस)

---

## 4. कैसे हिस्से जुड़ते हैं (Interfaces व Tensor Shapes)

सभी ब्लॉक **समान मॉडल आयाम `D`** साझा करते हैं जब तक प्रोजेक्शन न हो। इससे मॉड्यूल बदलना आसान रहता है।

### उच्च-स्तरीय इंटरफ़ेस अनुबंध

| ब्लॉक | इनपुट आकार | आउटपुट आकार | मुख्य तर्क |
|-------|------------|-------------|------------|
| Embedding | `(B, T)` या `(B, C, H, W)` | `(B, T, D)` | `vocab_size` / `patch_size`, `D` |
| Encoder Block | `(B, T, D)` + वैकल्पिक mask | `(B, T, D)` | `num_heads`, `ffn_dim`, `dropout` |
| Backbone (L ब्लॉक) | `(B, T, D)` | `(B, T, D)` | `num_layers=L` |
| Pooling | `(B, T, D)` | `(B, D)` | pooling प्रकार |
| Task Head | `(B, D)` या `(B, T, D)` | `(B, K)` या `(B, T, K)` | `num_classes=K` |

### मास्क व पैडिंग
- पैडेड टोकन के लिए **attention mask** बैकबोन तक भेजें ताकि पैडिंग पर ध्यान न जाए।
- आकार: `(B, T)` बूलियन / 0-1, या `(B, 1, 1, T)` ब्रॉडकास्ट योग्य रूप।

### अवशिष्ट कनेक्शन (Residual)
हर Encoder ब्लॉक में:
```
x ← x + Attention(Norm(x))
x ← x + FFN(Norm(x))
```
यह गहराई बढ़ाने पर ग्रेडिएंट प्रवाह स्थिर रखता है।

---

## 5. ट्रेनिंग बनाम इन्फरेंस नोट्स

### ट्रेनिंग (Training)
- **मोड:** `model.train()` — Dropout व BatchNorm सक्रिय
- **लॉस उदाहरण:**
  - वर्गीकरण: Cross-Entropy
  - मल्टी-लेबल: BCEWithLogits
  - अनुक्रम लेबलिंग: टोकन-वाइज Cross-Entropy (पैडिंग ignore_index)
- **ऑप्टिमाइज़र:** AdamW (weight decay ~0.01)
- **शेड्यूलर:** Cosine decay + warmup (कुल स्टेप्स का ~5–10%)
- **रेगुलराइज़ेशन:** Dropout, Label smoothing (वैकल्पिक), Gradient clipping (`max_norm=1.0`)
- **मिक्स्ड प्रिसिजन:** AMP (fp16/bf16) से गति व मेमोरी लाभ
- **चेकपॉइंट:** सबसे अच्छा validation मैट्रिक सहेजें

### इन्फरेंस (Inference)
- **मोड:** `model.eval()` + `torch.no_grad()`
- Dropout बंद; निर्धारक आउटपुट
- बैच आकार बढ़ाकर थ्रूपुट अनुकूलित करें
- वैकल्पिक: TorchsScript / ONNX निर्यात, क्वांटाइज़ेशन
- अनुक्रम कार्यों में length mask अभी भी आवश्यक

### साझा सावधानियाँ
| विषय | ट्रेनिंग | इन्फरेंस |
|------|----------|----------|
| Dropout | चालू | बंद |
| Stochastic depth | चालू (यदि हो) | बंद |
| Softmax | लॉस के अंदर | स्पष्ट रूप से आउटपुट के लिए |
| मास्क | आवश्यक | आवश्यक (पैडिंग पर) |

---

## 6. विस्तार कैसे करें (Specialization)

### 6.1 Vision में विशेषज्ञता
1. **इनपुट:** छवि `(B, 3, H, W)`  
2. **एम्बेडिंग:** Patch Embedding (ViT) या CNN Stem  
3. **बैकबोन:** ViT Encoder / ConvNeXt / ResNet  
4. **पूलिंग:** CLS टोकन या GAP  
5. **हेड:** `Linear(D → K)` इमेज क्लासिफिकेशन के लिए  
6. **अतिरिक्त:** Detection/Segmentation के लिए FPN + अलग डिकोड हेड जोड़ें

### 6.2 NLP में विशेषज्ञता
1. **इनपुट:** टोकन आईडी `(B, T)` + attention mask  
2. **एम्बेडिंग:** Token + Segment + Position Embeddings  
3. **बैकबोन:** Transformer Encoder (BERT-शैली) या Decoder-only (GPT-शैली)  
4. **पूलिंग:** CLS / mean pooling  
5. **हेड:**  
   - सेंटेंस क्लासिफिकेशन → `(B, K)`  
   - टोकन क्लासिफिकेशन → `(B, T, K)`  
   - जनरेशन → LM हेड `Linear(D → vocab)`  
6. **अतिरिक्त:** Pretrained tokenizer व weights से ट्रांसफर लर्निंग

### 6.3 अन्य विस्तार सुझाव
- **मल्टी-टास्क:** साझा बैकबोन + कई हेड (सहायक लॉस के साथ)
- **मल्टीमॉडल:** अलग vision/text एन्कोडर → क्रॉस-अटेंशन फ़्यूज़न → संयुक्त हेड
- **छोटा / एज डिप्लॉयमेंट:** `D` व `L` घटाएँ, knowledge distillation, depthwise convolutions
- **लंबे अनुक्रम:** Sliding window attention, ALiBi, या Linformer-शैली अनुमान

---

## 7. न्यूनतम संदर्भ कॉन्फ़िगरेशन (Blueprint Defaults)

```yaml
model:
  name: MEBH-Base
  dim: 512
  num_layers: 8
  num_heads: 8
  ffn_ratio: 4
  dropout: 0.1
  pooling: attention   # या: cls | mean | gap
  head: classification
  num_classes: 10      # अपने कार्य के अनुसार बदलें

training:
  optimizer: AdamW
  lr: 3.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.05
  epochs: 50
  batch_size: 32
  amp: true
```

---

## 8. निष्कर्ष

यह **Modular Encoder–Backbone–Head** डिज़ाइन एक स्पष्ट, आधुनिक, और विस्तारयोग्य ब्लूप्रिंट है। आप:

1. एम्बेडिंग को अपने डेटा प्रकार के अनुसार बदलें  
2. बैकबोन की गहराई / चौड़ाई संसाधन के अनुसार सेट करें  
3. केवल टास्क हेड बदलकर नया कार्य जोड़ें  

आगे का कदम: अपने डोमेन (Vision या NLP) चुनकर धारा 6 के अनुसार विशेषज्ञ मॉड्यूल प्लग करें, फिर उपरोक्त YAML को आधार बनाकर ट्रेनिंग शुरू करें।

---

*दस्तावेज़ भाषा: हिन्दी | आर्किटेक्चर कोड नाम: MEBH v1.0*
